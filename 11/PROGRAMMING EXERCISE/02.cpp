// 크기와 각도가 더 이상 데이터 성분으로 저장되지 않도록 Vector 클래스의 헤더 파일과 구현 파일(Listings 11.13과 11.14)을 수정하라.
// 대신, 그들은 magval()과 angval() 메서드가 호출될 때 즉시 계산되어야 한다.
// public 인터페이스는 변경하지 않고 그대로 두고(동일한 메서드들이 동일한 매개변수들을 사용해야 한다), private 메서드의 일부와 그 메서드 구형을 포함하여, private 부분을 변경해야 한다.
// Vector 클래스의 public 인터페이스가 변경되지 않기 때문에 변경이 없는 Listing 11.15를 사용하여, 수정된 버전을 테스트하라. (vect1.h, vect1.cpp, randwalk.cpp)