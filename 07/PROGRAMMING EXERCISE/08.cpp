// array 클래스를 사용하지 않고 Listing 7.15를 다시 해 본다. 아래 두 가지 경우로 진행한다.
// a. 계절명에 대해 const char *로 배열을 사용하여라.
//    그리고, 그 비용에 대하여 double로 array 객체를 사용하여라.
// b. 계절명에 대해 const char *로 배열을 사용하여라. 
//    그리고, 그 비용에 대하여 double로 배열을 사용하며, 그 배열을 단일 멤버로 가지는 구조체를 사용하라. (본 설계는 array 클래스의 기초 설계와 유사하다.)