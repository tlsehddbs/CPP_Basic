// Listing 7.7에서 세 개의 배열 처리 함수가 범위를 나타내는 두 개의 포인터 매개변수를 사용하도록 수정하라.
// 실제로 입력받은 개수를 리턴하는 대신에, fill_array() 함수는 배열에서 마지막으로 채워진 곳 바로 다음 위치를 지시하는 포인터를 리턴해야 한다. 
// 다른 두 함수는 이 포인터를 두 번째 매개변수로 사용하여 데이터릐 끝을 판단해야 한다.